{ "version": "2.13.226", "build": "20260614-nav-transicion-instantanea(causa-real del pegado:will-change:transform PERMANENTE en cada .guia-card/.pre-card=decenas de capas GPU reconstruidas en cada nav + backdrop-filter:blur por card=compositing caro e imperceptible + render de lista SINCRONO en el click.FIX:will-change/blur solo durante la animacion de entrada(look premium intacto via gradiente casi-opaco+halo solido);render de lista diferido setTimeout(0) tras el paint;slide-up GPU-only .18s auto-limpiante en animationend+reflow para re-disparar+prefers-reduced-motion.La vista aparece al instante,la lista llena un tick despues)" }
